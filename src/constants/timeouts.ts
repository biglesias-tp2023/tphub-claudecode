/** Named timeout constants to replace magic numbers. */

/** Session check timeout in auth store (ms) */
export const SESSION_CHECK_TIMEOUT_MS = 5000;

/** Toast/notification auto-dismiss duration (ms) */
export const NOTIFICATION_DURATION_MS = 5000;

/** Data freshness indicator update interval (ms) */
export const FRESHNESS_UPDATE_INTERVAL_MS = 30000;
